
cmp_to_map (moved to src)
---------------------------------

The conversion module was moved to `src/cmp_to_map.py` and can be run as a script or imported.

Usage (from project root):

python -m src.cmp_to_map data/SN\ 4566-004410.cmp results/output.map

Or import the functions from `src.cmp_to_map` in Python.
# Scripts

Put your standalone scripts (Python and bash) as well as jupyter notebooks here.